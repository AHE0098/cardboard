{
  "name": "cardboard",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node tests/battle-permissions.test.js && node tests/room-lifecycle.test.js",
    "check:shared": "node scripts/validate-shared.js",
    "smoke:shared-load": "node -e \"const {loadSharedDefinitions}=require('./shared/loadSharedDefinitions'); const r=loadSharedDefinitions({rootDir:'./shared'}); if(!r.found||r.error) throw r.error||new Error('definitions missing'); console.log('loaded definitions', r.data.version);\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.8.1"
  }
}
